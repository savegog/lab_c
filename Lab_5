#include <stdio.h>
#include <stdlib.h>
double* matrix_operation(double *matrix1, double *matrix2, int n, char op){
double *result = 
   if(op == '+') {
        for(int i = 0; i < n; i++) {
            result[i] = matrix1[i] + matrix2[i];
        }
    }
  if(op == '-') {
        for (int i = 0; i<n; i++){
          result[i]= matrix1[i]-matrix[2];
        }
  }
  if(op == '*'){
    for (int i=0; i<n; i++) {
      for (int j=0; j<n; j++){
        C[i][j] = 0;
            for (k=0; k<n; k++){
                result[i][j] += matrix1[i][k]*matrix2[k][j];
            }
      }
    }
  }
return result;
}
int main() {
    int n;
    char op;
    
    printf("Enter size matrices: ");
    scanf("%d", &n);
{функция +-* матрицы + выделение памяти}
main {ввести значения, ввести знак, вывести результат}
