#include <stdio.h>
#include <stdlib.h>

double* mo(double *m1, double *m2, int n, char op) {
    double *result = (double*)malloc(n * n * sizeof(double));
if(operation == '+') {
        for(int i = 0; i < n * n; i++) {
            result[i] = matrix1[i] + matrix2[i];
        }
